% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/met_cor.R, R/metabolite_set.R
\name{metabolite_set}
\alias{metabolite_set}
\title{Create a metabolite_set}
\usage{
metabolite_set(data, metabolites = NULL, pattern = NULL, id_cols = NULL)

metabolite_set(data, metabolites = NULL, pattern = NULL, id_cols = NULL)
}
\arguments{
\item{data}{A data.frame in wide format (one row per sample, one column per
metabolite, plus any metadata columns).}

\item{metabolites}{Optional explicit character vector of metabolite columns.}

\item{pattern}{Optional regex matched against column names.}

\item{id_cols}{Optional names of metadata columns; the remaining numeric
columns are taken to be metabolites.}
}
\value{
An object of class \code{"metabolite_set"}.

An object of class \code{"metabolite_set"}.
}
\description{
User-facing constructor. Resolves the metabolite columns from one of three
strategies, then validates the result.

User-facing constructor. Resolves the metabolite columns from one of three
strategies, then validates the result.
}
\examples{
df <- data.frame(
  sample_id = c("a", "b", "c"),
  group     = c("x", "y", "x"),
  glucose   = c(0, 2.1, 3.4),
  lactate   = c(1.2, 0, 0.9)
)
ms <- metabolite_set(df, id_cols = c("sample_id", "group"))
ms
df <- data.frame(
  sample_id = c("a", "b", "c"),
  group     = c("x", "y", "x"),
  glucose   = c(0, 2.1, 3.4),
  lactate   = c(1.2, 0, 0.9)
)
ms <- metabolite_set(df, id_cols = c("sample_id", "group"))
ms
}
